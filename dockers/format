#pragma once
#include <fmt/format.h>

namespace std
{
using fmt::format;
using fmt::format_to;

template <typename... Args>
using format_string = fmt::format_string<Args...>;

template <typename T, typename CharT = char>
struct formatter : fmt::formatter<T, CharT>
{};
} // namespace std
